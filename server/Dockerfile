FROM python:3.9-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

# Environment variable for the port, defaults to 8000
ENV PORT=8000

# Run the application
CMD uvicorn main:app --host 0.0.0.0 --port $PORT
